// Code your solutions in this file
function writeCards(array, event ){
  let arr = []
  for(let i = 0; i < array.length; i++){
    arr[i] = `Thank you, ${array[i]}, for the wonderful ${event} gift!`;
  };
};